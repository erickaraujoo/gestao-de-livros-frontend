import styled from 'styled-components';
import { theme, fontSizes } from '../../styles/global';

export const ErrorLoginInfo = styled.p`
  color: ${theme.colors.red};
  padding-top: 10px;
  font-size: ${fontSizes.default};
`;
